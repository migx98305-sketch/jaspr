// PlayerCurrency.h
#pragma once
#include "CoreMinimal.h"
#include "GameFramework/PlayerController.h"
#include "PlayerCurrency.generated.h"

// Bu sinf foydalanuvchi pulini saqlash va boshqarish uchun
UCLASS()
class YOURGAME_API APlayerCurrency : public APlayerController
{
    GENERATED_BODY()

public:
    // Foydalanuvchi puli (UZS)
    UPROPERTY(BlueprintReadWrite, EditAnywhere, Category="Pul")
    int32 UZS = 0;

    // Pul qo‘shish funksiyasi
    UFUNCTION(BlueprintCallable, Category="Pul")
    void AddCurrency(int32 Amount);

    // Pul sarflash funksiyasi
    UFUNCTION(BlueprintCallable, Category="Pul")
    bool SpendCurrency(int32 Amount);
};
